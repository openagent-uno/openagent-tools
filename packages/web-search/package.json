{
  "name": "openagent-web-search-mcp",
  "version": "1.0.0-beta.1",
  "description": "Web search MCP server with multi-engine search and content extraction (no API key needed)",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "openagent-web-search-mcp": "dist/index.js"
  },
  "scripts": {
    "start": "npm run build && node dist/index.js",
    "build": "tsc",
    "test": "npm run build && node --test test/*.test.mjs",
    "clean": "rm -rf dist",
    "install-browser": "playwright install chromium"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.15.0",
    "axios": "^1.6.8",
    "cheerio": "^1.0.0-rc.12",
    "p-limit": "^6.2.0",
    "p-retry": "^6.2.1",
    "playwright": "^1.48.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ]
}
